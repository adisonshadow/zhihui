# 短漫剧剧本元素说明

本文档系统梳理**短漫剧**（短篇漫画/漫剧视频）剧本所涉及的各类元素，以便评估后续产品开发目标与功能优先级。按逻辑层次组织，尽量覆盖全面。

---

## 1. 结构层次元素

| 元素 | 说明 | 常见粒度 |
|------|------|----------|
| **项目 / 漫剧** | 一整部作品，可含多集 | 1 个 |
| **集（Episode）** | 按播出/阅读单元划分的大段落，如「第 1 集」「第 2 集」 | 若干集 |
| **场景（Scene）** | 时间、空间连续的一段戏，同一场景内一般不换景 | 若干场景/集 |
| **镜头 / 分镜（Shot）** | 单个机位或单格画面，镜头切换即分镜切 | 若干镜头/场景 |
| **镜头组（Sequence）** | 若干镜头组成的有意编排的小序列，可视为「微型蒙太奇」 | 可选 |
| **节拍（Beat）** | 剧情中的一个情绪/动作/信息单位，时长可短可长 | 微观 |

**关系示意**：项目 → 集 → 场景 → 镜头（分镜） → 节拍

---

## 2. 叙事元素

| 元素 | 说明 | 示例 |
|------|------|------|
| **剧情概要 / 大纲** | 集或场景的摘要描述，用于整体把控 | 「小明在教室发现神秘信封」 |
| **剧本文本** | 详细台词、旁白、动作描写，可直接用于分镜与配音 | 对白 + 动作说明 |
| **故事线（Storyline）** | 单条叙事线，如主线、支线、角色线 | 主线、回忆线 |
| **情节点（Plot Point）** | 推进剧情的关键转折 | 揭秘、冲突爆发 |
| **伏笔与回收** | 前文铺垫与后文呼应 | 道具、台词、设定 |
| **冲突类型** | 人 vs 人、人 vs 环境、人 vs 自我、人 vs 命运等 | 用于 AI 生成时的约束 |
| **悬念 / 钩子** | 吸引观众继续看的设置 | 集末悬念、场景内悬念 |
| **节奏控制** | 快慢、张弛、高潮分布 | 紧张段 / 放松段交替 |

---

## 3. 人物相关元素

| 元素 | 说明 | 示例 |
|------|------|------|
| **角色（Character）** | 剧中有名有姓、有戏份的人物 | 主角、配角、龙套 |
| **角色绑定** | 集/场景与具体人物的关联，便于素材与 TTS 选择 | 第 1 集绑定：小明、小红、老师 |
| **角色弧（Character Arc）** | 角色在整部剧中的成长与变化 | 从胆小到勇敢 |
| **人物关系** | 角色之间的社会/情感关系 | 朋友、对手、师徒 |
| **对白（Dialogue）** | 角色说出的台词 | 「你好」「我不同意」 |
| **内心独白（Inner Monologue）** | 角色内心想法，不出口 | 画外音、气泡字 |
| **旁白（Narration）** | 叙述者讲述，非角色直接发言 | 全知视角、第一人称 |
| **语气 / 情绪** | 对白的情绪标记，影响 TTS 与表演 | 愤怒、悲伤、调侃 |
| **说话对象** | 对白针对谁，影响镜头与人物站位 | A 对 B 说 |
| **沉默 / 停顿** | 无台词时段，用于情绪或节奏 | Beat、留白 |

---

## 4. 时间与节奏元素

| 元素 | 说明 | 示例 |
|------|------|------|
| **时长（Duration）** | 单个素材/镜头/场景的持续时间（秒） | 3 秒、10 秒 |
| **时间顺序** | 顺叙、倒叙、插叙、闪回 | 影响镜头编排 |
| **时间跳跃** | 场景内或跨场景的时间省略 | 「三年后」「次日」 |
| **实时 vs 压缩** | 故事时间与播放时间的关系 | 1 分钟演 1 小时 |
| **节奏标记** | 快切、长镜头、慢动作等 | 影响剪辑风格 |
| **节拍时长** | 一个情绪/信息单位的建议时长 | 用于自动估算分镜时长 |

---

## 5. 空间与场景元素

| 元素 | 说明 | 示例 |
|------|------|------|
| **场景类型** | 室内/室外、日/夜、具体地点 | 教室、街道、森林 |
| **场景描述** | 场景的视觉与氛围说明 | 「昏暗的教室，窗外下雨」 |
| **场景转换** | 切、淡入淡出、划像等 | 影响镜头衔接 |
| **空间关系** | 角色与物品的相对位置 | 前景、中景、背景 |
| **景深 / 层次** | 前景、中景、背景的区分与虚化 | 用于镜头与景别 |
| **环境氛围** | 光影、天气、季节、时代感 | 影响视觉与音乐 |

---

## 6. 镜头与视觉元素

| 元素 | 说明 | 示例 |
|------|------|------|
| **景别** | 远景、全景、中景、近景、特写 | 决定画面信息量 |
| **机位** | 俯拍、仰拍、平视、过肩 | 影响视角与情绪 |
| **镜头运动** | 推、拉、摇、移、跟、升、降 | 对应镜头 Pan/Zoom/Tracking |
| **构图** | 三分法、对称、黄金分割、留白 | 影响画面美感 |
| **焦点 / 主体** | 画面重点，可配合虚化 | 人物、物品 |
| **光影** | 主光、辅光、轮廓光、阴影 | 氛围与情绪 |
| **色彩** | 色调、对比、饱和度、情绪色 | 冷暖、黑白、高饱和 |
| **转场效果** | 切、溶、划、擦、3D 翻转等 | 场景/镜头衔接 |

---

## 7. 动作与表演元素

| 元素 | 说明 | 示例 |
|------|------|------|
| **角色动作** | 走、跑、坐、手势、表情变化 | 对应精灵图、骨骼动画 |
| **动作脚本** | 可被解析并驱动动画的指令序列 | 「走路 3 步 → 转身 → 挥手」 |
| **预设动作** | 跑步、跳跃、挥手、点头等可复用动作 | 骨骼人物一键应用 |
| **表情** | 喜、怒、哀、惊、恐等 | 表情贴图、精灵帧 |
| **视线方向** | 角色看向哪里 | 影响镜头与观众注意力 |
| **肢体语言** | 姿态、手势传达情绪 | 与对白配合 |

---

## 8. 音频元素

| 元素 | 说明 | 示例 |
|------|------|------|
| **对白配音（Voice-over）** | 角色对白的 TTS 或真人配音 | 与剧本对白一一对应 |
| **旁白配音** | 叙述者旁白的 TTS 或配音 | 全知视角讲述 |
| **背景音乐（BGM）** | 场景/情绪配乐 | 紧张、温馨、悬疑 |
| **音效（Sound Effect）** | 动作、环境、 UI 等声音 | 脚步声、关门、雷雨 |
| **静音** | 刻意无声音片段 | 增强张力 |
| **音量与混音** | 各轨相对音量与空间感 | 对白突出、BGM 压低 |

---

## 9. 素材与资产元素

| 元素 | 说明 | 分类示例 |
|------|------|----------|
| **人物素材** | 角色形象、精灵图、骨架人物 | 人物 |
| **场景背景** | 山水、室内、天空等 | 场景背景 |
| **情景道具** | 可与剧情联动的物品 | 马车、桌椅、武器 |
| **声效素材** | 音效文件 | 声效 |
| **音乐素材** | BGM 等 | 音乐 |
| **透明视频特效** | 带 Alpha 通道的视频 | 透明视频特效 |
| **贴纸** | 静态或简单动效 | 贴纸 |
| **素材状态** | 同一素材的不同形态 | 马车：行驶中 / 停止 |
| **素材 Tag** | 便于筛选与批量的标签 | 主角、Q 版、古风 |

---

## 10. 元信息与描述元素

| 元素 | 说明 | 用途 |
|------|------|------|
| **标题** | 集/场景/镜头的名称 | 导航、检索 |
| **摘要 / 概要** | 简短描述 | 预览、AI 上下文 |
| **备注** | 制作说明、待办、版本说明 | 协作、迭代 |
| **标签（Tag）** | 类型、风格、主题等 | 筛选、推荐 |
| **版本号 / 修订** | 剧本迭代记录 | 协作、回溯 |
| **创作元数据** | 作者、日期、修改记录 | 项目管理 |

---

## 11. 类型与风格元素

| 元素 | 说明 | 示例 |
|------|------|------|
| **题材** | 爱情、悬疑、搞笑、奇幻、日常等 | 影响 AI 生成与素材 |
| **风格** | 写实、Q 版、日漫、国风、赛博等 | 画风、配色 |
| **基调** | 轻松、严肃、悲情、热血 | 整体情绪方向 |
| **目标受众** | 年龄、性别、兴趣 | 影响尺度与表达 |
| **时长定位** | 超短（1 分钟内）、短（数分钟）、中（10 分钟级） | 影响节奏与信息量 |

---

## 12. 交互与分支元素（可选扩展）

| 元素 | 说明 | 示例 |
|------|------|------|
| **分支点** | 观众选择导致不同走向 | 选项 A / 选项 B |
| **多结局** | 根据选择或条件触发不同结局 | 好结局 / 坏结局 |
| **可点击热区** | 画面内可交互区域 | 点击查看详情 |
| **进度标记** | 已看过/未看、完成度 | 续看、收藏 |

---

## 13. 与产品的对照（便于评估开发目标）

| 产品模块 | 已支持或计划支持的剧本元素 |
|----------|----------------------------|
| **剧情大纲** | 集、概要、剧本文本、角色绑定、AI 剧本生成 |
| **人物设计** | 角色、形象、多角度、预设动作、TTS 默认参数 |
| **AI 配置** | 剧本风格、画风偏好、自定义提示 |
| **视频设计器** | 场景、镜头、分镜、素材条、关键帧、镜头运动 |
| **素材体系** | 人物、场景、道具、声效、音乐、特效、贴纸、状态 |
| **导出** | 画布内内容、横竖屏、时间线顺序 |

**可优先考虑的扩展方向**（按剧本元素）：  
- 对白 → 台词条、说话人标记、TTS 自动绑定  
- 镜头 → 景别、机位、镜头运动关键帧  
- 节奏 → 节拍时长、自动分镜时长估算  
- 动作 → 动作脚本解析、预设动作与剧本联动  
- 元信息 → 标签、备注、版本管理  

---

## 14. 元素关系示意（简化）

```
项目
 └─ 集（Episodes）
      ├─ 概要
      ├─ 剧本文本
      ├─ 角色绑定
      └─ 场景（Scenes）
           ├─ 场景描述
           ├─ 镜头 / 分镜（Shots）
           │    ├─ 景别、机位、运动
           │    ├─ 时长
           │    └─ 素材条（Blocks）
           │         ├─ 人物 / 背景 / 道具 / 声效 / 音乐
           │         ├─ 对白（可绑定 TTS）
           │         └─ 关键帧（位置、缩放、旋转、骨骼等）
           └─ 转场
```

---

## 15. 剧本标准结构（AI 输出与可修改范围）

本节定义**结构化剧本**的标准格式，用于：① 指导 AI 按此模型输出；② 本应用的剧本编辑与语音/画面设计；③ 用户指定 AI 修改某一具体部分。  
结构由多个子结构组成，每个子结构有可寻址的 **path**（如 `episode.1.scene.2.dialogue.1`），便于精确定位修改。  

**当前设计：暂不需要节拍（Beat）**。场景直接包含对白、旁白、动作，保证数据结构完整；path 为 `episode.{i}.scene.{j}.dialogue|narration|action.{k}`。节拍（Beat）为未来可选扩展，引入后对白/旁白/动作可下沉到节拍层级。

---

### 15.1 顶层结构：剧本（Script）

```json
{
  "id": "script_xxx",
  "title": "神秘信封",
  "summary": "高中生小明在教室发现神秘信封，展开一段悬疑冒险。",
  "episodes": [ /* Episode[] */ ],
  "characters": [ /* CharacterRef[] */ ],
  "tags": ["悬疑", "校园", "成长"]
}
```

| 字段 | 说明 |
|------|------|
| id | 剧本唯一标识 |
| title | 剧本标题 |
| summary | 全剧概要 |
| episodes | 集列表，有序 |
| characters | 本剧角色引用列表 |
| tags | 题材、风格等标签 |

---

### 15.2 子结构一：集（Episode）

戏剧效果标签不绑定剧集（粒度太大），仅绑定场景。

```json
{
  "id": "ep_1",
  "path": "episode.1",
  "title": "第 1 集",
  "summary": "小明在空教室发现桌上的神秘信封，拆开后看到奇怪符号。",
  "characterRefs": ["小明", "小红"],
  "scenes": [ /* Scene[] */ ]
}
```

| 字段 | 说明 |
|------|------|
| id | 集唯一标识 |
| path | 可寻址路径，如 `episode.1` |
| title | 集标题 |
| summary | 本集概要 |
| characterRefs | 本集出场角色 ID 列表 |
| scenes | 场景列表，有序 |

---

### 15.3 子结构二：场景（Scene）

**当前设计**：场景直接包含对白、旁白、动作（暂不需要节拍，见 15.4）。

```json
{
  "id": "scene_1_1",
  "path": "episode.1.scene.1",
  "title": "空教室",
  "summary": "放学后，空无一人的教室，小明发现信封。",
  "location": "教室",
  "timeOfDay": "傍晚",
  "atmosphere": "安静、略带神秘",
  "actions": [ /* Action[] */ ],
  "dialogues": [ /* Dialogue[] */ ],
  "narrations": [ /* Narration[] */ ],
  "transition": "切",
  "dramaTags": ["悬念"]
}
```

| 字段 | 说明 |
|------|------|
| id | 场景唯一标识 |
| path | 可寻址路径，如 `episode.1.scene.1` |
| title | 场景标题 |
| summary | 场景概要 |
| location | 地点 |
| timeOfDay | 日/夜/黄昏等 |
| atmosphere | 氛围描述 |
| actions | 动作/舞台说明列表（当前直接挂在场景下） |
| dialogues | 对白列表（当前直接挂在场景下） |
| narrations | 旁白列表（当前直接挂在场景下） |
| transition | 与上一场景的转场方式 |
| dramaTags | 戏剧效果标签 |

---

### 15.4 子结构三：节拍（Beat）——当前暂不使用

节拍是最小可编辑单元，内部可含**对白**、**旁白**、**动作**等，彼此独立，便于单独修改。

**当前设计**：暂不需要节拍，对白/旁白/动作直接挂在场景下（见 15.3）。节拍为**未来可选扩展**，引入后数据结构为：Scene → Beat → (Dialogues, Narrations, Actions)。

```json
{
  "id": "beat_1_1_1",
  "path": "episode.1.scene.1.beat.1",
  "summary": "小明发现桌上的信封",
  "durationHint": 5,
  "actions": [ /* Action[] */ ],
  "dialogues": [ /* Dialogue[] */ ],
  "narrations": [ /* Narration[] */ ],
  "dramaTags": []
}
```

| 字段 | 说明 |
|------|------|
| id | 节拍唯一标识 |
| path | 可寻址路径，如 `episode.1.scene.1.beat.1` |
| summary | 节拍概要（动作/信息要点） |
| durationHint | 建议时长（秒），用于分镜估算 |
| actions | 动作/舞台说明列表 |
| dialogues | 对白列表（单独结构化） |
| narrations | 旁白列表（单独结构化） |
| dramaTags | 戏剧效果标签 |

---

### 15.5 子结构四：对白（Dialogue）——单独结构化

对白与旁白分开存储，便于 TTS 绑定、角色筛选与局部修改。

```json
{
  "id": "dlg_1_1_1_a",
  "path": "episode.1.scene.1.dialogue.1",
  "speaker": "小明",
  "target": "小红",
  "text": "你相信这世上真的存在魔法吗？",
  "emotion": "犹豫",
  "volume": "正常",
  "order": 1
}
```

| 字段 | 说明 |
|------|------|
| id | 对白唯一标识 |
| path | 可寻址路径 |
| speaker | 说话人（角色 ID） |
| target | 说话对象（角色 ID，可选） |
| text | 台词文本 |
| emotion | 语气/情绪（影响 TTS） |
| volume | 音量暗示（正常/轻声/大喊等） |
| order | 在同一场景内的顺序 |

**简单示例**：
```
场景内：
  对白 1：小明 →（无对象）「这是什么？」
  对白 2：小红 → 小明「好像是一封信，你拆开看看。」
```

---

### 15.6 子结构五：旁白（Narration）——单独结构化

旁白与对白分离，便于区分叙述者视角与 TTS 配置。

```json
{
  "id": "nar_1_1_2_a",
  "path": "episode.1.scene.1.narration.1",
  "narratorType": "全知",
  "text": "那是 1999 年的夏天，谁也没想到，这个信封会改变一切。",
  "emotion": "怀念",
  "order": 1
}
```

| 字段 | 说明 |
|------|------|
| id | 旁白唯一标识 |
| path | 可寻址路径 |
| narratorType | 全知 / 第一人称主角 / 第一人称配角 |
| text | 旁白文本 |
| emotion | 叙述情绪 |
| order | 在同一场景内的顺序 |

**简单示例**：
```
场景内：
  旁白 1：（全知）「小明从未想过，平静的校园生活会在这一天被打破。」
  对白 1：小明 → 小红「你看，这里面有张奇怪的图。」
```

---

### 15.7 子结构六：动作（Action）

描述角色动作、环境变化等非台词内容，用于分镜与动画参考。

```json
{
  "id": "act_1_1_1_a",
  "path": "episode.1.scene.1.action.1",
  "description": "小明走近课桌，迟疑地伸手触碰信封",
  "characters": ["小明"],
  "order": 1
}
```

| 字段 | 说明 |
|------|------|
| id | 动作唯一标识 |
| path | 可寻址路径 |
| description | 动作/舞台说明文本 |
| characters | 涉及角色 |
| order | 顺序 |

---

### 15.8 戏剧效果标签（dramaTags）——Tag 化

冲突、反转、悬念等以**标签**形式附着在**场景**上（剧集粒度过大，不绑定），便于筛选、统计与 AI 约束。

| 标签 key | 说明 | 可附着层级 |
|----------|------|-------------|
| conflict | 冲突 | 场景 |
| reversal | 反转 | 场景 |
| suspense | 悬念 | 场景 |
| climax | 高潮 | 场景 |
| hook | 钩子（吸引续看） | 场景 |
| foreshadow | 伏笔 | 场景 |
| payoff | 回收/呼应 | 场景 |
| tension | 紧张 | 场景 |
| relief | 舒缓 | 场景 |
| comedy | 喜剧点 | 场景 |
| tearjerker | 催泪点 | 场景 |

**冲突类型子标签**（可与 conflict 组合）：
- `conflict.person` 人 vs 人
- `conflict.environment` 人 vs 环境
- `conflict.self` 人 vs 自我
- `conflict.fate` 人 vs 命运

**示例**：
```json
"dramaTags": ["suspense", "hook", "conflict.person"]
```

---

### 15.9 角色引用（CharacterRef）

剧本层维护角色引用，便于绑定人物素材与 TTS。

```json
{
  "id": "小明",
  "name": "小明",
  "role": "主角",
  "description": "高中生，好奇心强，性格内向"
}
```

---

### 15.10 用户指挥 AI 修改的指令示例

基于上述可寻址 path，用户可精确定位要修改的部分：

| 用户指令 | 对应 path / 范围 |
|----------|------------------|
| 「把第 1 集第 2 场景的对白改得更搞笑一点」 | `episode.1.scene.2` 下所有 `dialogues` |
| 「修改小明在场景 2 的台词」 | `episode.1.scene.2` 下 `dialogues` 且 `speaker=小明` |
| 「这段旁白太长了，缩短一半」 | 指定 `narration.id` 或 `path` |
| 「在场景 2 加一个反转」 | `episode.1.scene.2`，添加/调整 `dramaTags: ["reversal"]` 并补充内容 |
| 「把场景 2 的悬念强化」 | `episode.1.scene.2`，调整 `dramaTags` 与相关 `narrations`、`dialogues` |
| 「增加小红与老师的冲突」 | 定位含 `conflict.person` 的场景，或新建场景并设 `dramaTags` |

**推荐指令格式**（可解析为结构化请求）：
```
修改 [path] 的 [dialogues|narrations|actions|dramaTags|summary]：具体修改要求
```

---

### 15.11 完整示例（片段）

```json
{
  "title": "神秘信封",
  "episodes": [{
    "id": "ep_1",
    "path": "episode.1",
    "title": "第 1 集",
    "summary": "小明在空教室发现神秘信封。",
    "scenes": [{
      "id": "scene_1_1",
      "path": "episode.1.scene.1",
      "title": "空教室",
      "location": "教室",
      "timeOfDay": "傍晚",
      "dramaTags": ["suspense"],
      "dialogues": [{ "speaker": "小明", "text": "这是什么？", "emotion": "疑惑", "order": 1 }, { "speaker": "小红", "target": "小明", "text": "好像是一封信，你拆开看看。", "emotion": "好奇", "order": 2 }],
      "narrations": [{ "narratorType": "全知", "text": "谁也没想到，这个信封会改变一切。", "emotion": "怀念", "order": 1 }],
      "actions": [{ "description": "小明走近课桌，发现一个牛皮纸信封", "characters": ["小明"], "order": 1 }]
    }]
  }],
  "characters": [{ "id": "小明", "name": "小明", "role": "主角" }, { "id": "小红", "name": "小红", "role": "配角" }]
}
```

---

以上为标准剧本结构定义，AI 可按此输出，剧本编辑与语音/画面设计可据此解析与联动，用户可通过 path 精确指定修改范围。
