---
description: 芝绘项目开发约束与 Cursor 必须遵守的规则
globs: 
alwaysApply: true
---

# 芝绘 (Yiman) 项目规则

## 文档权威来源

- 需求与界面：`docs/01-功能文档.md`
- 技术约束：`docs/02-技术文档.md`
- 任务列表：`docs/03-开发计划和进度.md`
- AI 协作：`docs/04-AI开发文档.md`
- 开发启动（可直接按此开始）：`docs/05-开发启动指引.md`

## 添加依赖（必须牢记，不得违反）

**禁止通过直接修改 `package.json` 来添加任何依赖。**

正确流程：在终端先设置代理，再使用 yarn 安装，以保证包版本存在且最新。

```bash
export http_proxy=http://127.0.0.1:7897
export https_proxy=http://127.0.0.1:7897
yarn add <包名>
```

多包示例：`yarn add @ant-design/x @ant-design/x-markdown @ant-design/x-sdk`。

当用户或任务要求「添加一个包」时，应回复将执行上述命令并给出具体 `yarn add` 示例，**不要**贴出对 package.json 的 diff 或直接编辑 package.json。

## 技术栈速记

- 构建：Vite；包管理：Yarn
- UI：Ant Design，仅 dark 主题；可 Resize 布局：Ant Design Splitter
- AI Chat：Ant Design X 三件套（@ant-design/x、@ant-design/x-markdown、@ant-design/x-sdk）
- 参考项目（路由、Ant Design 主题、Tailwind）：`/Users/yanfang/dev/Biezhi2/web`
- 测试命令：`yarn test`

## 任务与注释

- 接任务时先确认对应「功能文档 x.x / 技术文档 x.x / 开发计划 2.x」
- 关键逻辑注释注明文档出处（如：`// 见功能文档 6.3`）
- 完成开发计划中的 Todo 后，在 `docs/03-开发计划和进度.md` 中勾选对应项

## 开发注意事项
- 不要出现太大的文件，尽量将独立功能拆分成独立文件
- 在编写自定义样式时，请考虑 dark 主题的视觉效果，选择颜色时，确保在暗色背景下具有良好的对比度和可读性，自定义组件样式时，应与 dark 主题保持一致

## 🚫 Ant Design 使用规范
- **Modal**：禁止使用函数式 Modal 方法
- **Tag**：没有 `size` 属性
- **Space**：`direction` 已弃用，请用 `orientation`（如 `orientation="vertical"`）
- **Spin**：`tip` 仅在 nest 或 fullscreen 模式下生效，需用子元素包裹（如 `<Spin tip="…"><div style={{ minHeight: 120 }} /></Spin>`）
- **List**：已弃用，列表用 `Space`/`Flex` + `map` 或自定义结构实现
- **message/notification**：静态方法无法消费主题等 context，根组件需包一层 `<App>`，页面内用 `App.useApp()` 取 `message`/`notification` 使用
- **Splitter** `layout` is deprecated. Please use `orientation` instead.
- **InputNumber** `addonAfter` is deprecated. Please use `Space.Compact` instead.
- 如果有 Instance created by `useForm`，不要忘记 connected to Form element
