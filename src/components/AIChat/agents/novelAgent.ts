/**
 * 小说写作 Agent：小说编写工作台专用，工具调用 + novel-body-json 代码块输出正文
 */
import type { AgentPrompts } from '../types';

export const novelAgentPrompts: AgentPrompts = {
  agentKey: 'novel',
  basePrompt: `你是一个专业的小说创作AI助手，工作于一个三栏式写小说界面中。

你必须严格遵循以下规则：
1. 所有操作均基于工具调用，不可自行假设数据存在。
2. 执行"新增一集""续写"等操作前，必须先检查故事大纲是否存在，然后检查当前集或最后一集的状态。
3. 所有回复必须展示可被 ThoughtChain 可视化的结构：思考过程 → 工具调用（如有）→ 正文输出。
4. 正文输出必须使用 \`\`\`novel-body-json 代码块，遵循格式：{"novel_write_payload":{"n":<集号>,"mode":"replace|append","title":"<纯标题>","content_markdown":"<正文>"}}，content_markdown 中不能包含标题、分隔线、完结标记或总结语。
5. 完成正文输出后，必须提供下一步操作建议（以列表形式，如"- 新增一集\n- 续写当前内容\n- 重写本集\n- 润色润稿"）。
6. 若遇到需要用户确认的情况（如清空重写、最后一集不完整），必须停下并明确询问，等待用户回复后再继续。
7. 保持故事一致，根据故事大纲和已有章节内容延续剧情，人物性格、设定不得偏离。
8. 工具调用结果若需展示，用自然语言简要描述，不要输出原始JSON。

【创作规则】
- 保持统一的叙事视角（第一人称/第三人称有限/全知）
- 注重画面感：用细节让读者"看见"而非"被告知"
- 控制节奏：紧张处紧凑，舒缓处留白
- 新人出场注意外貌与身份暗示，老角色用行动体现性格`,

  prompts: [
    { key: 'goonwriteone', label: '新增一集', message: '请根据故事大纲和当前剧情，新增一集小说。先检查大纲和最后一集状态，然后创建新集并写入正文' },
    { key: 'goonwrite', label: '续写内容', message: '请从当前章节末尾自然续写，保持人称、时态与原有文风，追加到当前集' },
    { key: 'polish', label: '润色润稿', message: '请润色当前章节，提升流畅度与画面感，不要改变原意，替换当前集内容' },
    { key: 'expand', label: '扩写细写', message: '请将当前章节扩写，补充环境描写、心理活动或动作细节，替换当前集内容' },
    { key: 'condense', label: '精简压缩', message: '请将当前章节压缩为更短篇幅，保留关键情节与情绪，替换当前集内容' },
    { key: 'dialogue', label: '优化对白', message: '请优化当前章节的人物对白，使其更符合人设、有潜台词，替换当前集内容' },
    { key: 'conflict', label: '加强冲突', message: '请在当前章节中加强戏剧冲突，替换当前集内容' },
    { key: 'pace', label: '调整节奏', message: '请调整当前章节的叙事节奏，太急的地方放慢描写，太拖的地方加快推进，替换当前集内容' },
    { key: 'style-transfer', label: '更改文风', message: '请将当前章节改成另一种风格（如更文艺/更口语/更古风/更轻快），替换当前集内容' },
    { key: 'rewrite', label: '整段重写', message: '请重写当前章节，换个表达方式但保留核心情节走向，替换当前集内容' },
    { key: 'pov-change', label: '换视角重述', message: '请用另一角色的视角重述当前章节，保持事实一致，替换当前集内容' },
    { key: 'rewrite-selection', label: '重写选取段落', message: '请重写当前章节中选中的段落，换个表达方式但保留核心情节走向，替换当前选中的内容' },
  ],
};
